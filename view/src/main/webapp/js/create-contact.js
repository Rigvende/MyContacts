function createContact() {
    "use strict";

    //todo написать

    // var userName = document.getElementById("user_name").value;
    // var userLogin = document.getElementById("user_login").value;
    // var userEmail = document.getElementById("user_email").value;
    //
    // var xmlhttp = new XMLHttpRequest();   // new HttpRequest instance
    // xmlhttp.open("POST", "http://localhost:8080"); //прописать урл к контроллеру создания контактов
    // xmlhttp.setRequestHeader("Content-Type", "application/json");
    // xmlhttp.send(JSON.stringify({name: userName, login: userLogin, email: userEmail}));

    //todo создать попап об успешном завершении

    //вернуться к списку
    //окошко контакт сохранен => кнопка к списку контактов
}